diff --git a/es/blockLexer.mjs b/es/blockLexer.mjs
index 0a7c474f8dd832b83e898f4e9353db9dbf6e7a97..3a67575880b88f990094055f3435fe9724ba35c1 100644
--- a/es/blockLexer.mjs
+++ b/es/blockLexer.mjs
@@ -49,7 +49,7 @@ const createBlockLexer = () => {
 			frozenBlocks = [];
 		}
 		const rawTail = content.slice(frozenRaw.length);
-		const processedTail = remend(rawTail);
+		const processedTail = remend(rawTail, { htmlTags: false });
 		const tailTokens = marked.lexer(processedTail);
 		const tailBlocks = toBlocks(tailTokens, frozenRaw.length);
 		const result = {
